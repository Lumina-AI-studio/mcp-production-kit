# src/observability — OpenTelemetry + health (M1)

Empty on purpose; lands in M1. Planned: OTel traces/metrics (trace id is
propagated into audit events), `/healthz` liveness and `/readyz` readiness
endpoints.
